# PathMX Styling

<!-- TODO: draft. Ground truth: pathmx repo — plugins/core/{styles,theme}/
     readmes; .agents/skills/pathmx-authoring/references/styling-and-theming.md. -->

Source CSS, theme tokens, fonts, and color modes.

## Style imports

<!-- TODO: `[@styles]: ./x.css` (source-scoped), `[@styles.print]:` /
     `[@styles.screen]:` (media-scoped), `[@root.styles]: ./bundle.css`
     (root-bundle-wide, valid only in a root Source). Local `.css` only. -->

## Theme tokens

<!-- TODO: named `theme` frontmatter → `--pmx-*` variables: `color`, `prose`
     (size/leading/flow), `measure`, `code`, `light:`/`dark:` branches,
     `forceColorScheme`. OKLCH values preferred. -->

## Fonts

<!-- TODO: `fonts` frontmatter — hosted or local font-face registration
     (owned by the styles plugin). -->

## Color modes

<!-- TODO: `@dark {}` / `@light {}` blocks in authored CSS (including
     component CSS) instead of internal color-scheme hooks. -->
