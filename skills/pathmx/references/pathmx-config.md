# PathMX Config

<!-- TODO: draft. Ground truth: pathmx repo — pathmx.config.md at repo root,
     packages/build/src/plugins/core/pathmx/index.ts (default plugin stack). -->

Configuring a PathMX repository: entry roots, plugins, and build options.

## Config files

<!-- TODO: `pathmx.config.md` at the repo root; `pathmx.json` / package
     scripts; when each applies. -->

## Roots and entry points

<!-- TODO: default entry `paths/index.path.md`; specifying additional roots
     via CLI or config; one graph per root. -->

## Plugins

<!-- TODO: the default `pathmx()` stack; disabling core plugins
     (e.g. `{"plugins": {"theme": false}}`); opt-in plugins
     (code highlighting, tailwind, lucide icons, image). -->
